What you learned about NumPy.
It is used to work with arrays and have many built-in functions like .sum, .multiply, .sort which we would have to do manually
if we use lists instead of it. We can slice through the arrays and fetch whatever we want. We can use indexing.
I learnt how to work on arrays of different dimensions and slice through them and convert them to different dimensions through np.

What you learned about Pandas.
It is used to work on datasets and we worked on csv files today and loaded the downlaoded csv file in my program and fetched data from it.
I learn to filter out data using certain conditions and add further columns to the dataset and save the changes to another new csv file

Key insights you found from the dataset.
It had subject marks, i took average of the students for each subject and added the column to the dataset which was later use to 
find the top 5 students on the basis of their performance by finding the overall average.
Used the same average to find students who are below average
Found the total number of students and their features like age.
we can find the number of students who passed and failed by setting up the condition for marks being greater than 50%

Challenges you faced during implementation.
In numpy, i faced problem with indexing of multi-dimensional arrays and slicing them. Took some time to get comfortable 
with looping over the 3-d arrays.
In pandas, faced difficulty in changing the data and getting familiar with the type of data we are getting and how we can make changes in it.
Applying conditions was challenging too especially when finding average of each subject at once.
